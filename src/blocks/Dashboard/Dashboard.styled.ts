import styled from "styled-components";
import { largeFontSize, mainColor } from "@/styles";

export const DashboardLoading = styled.div`
  font-size: ${largeFontSize};
  color: ${mainColor};
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
`;
